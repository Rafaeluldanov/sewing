node_modules/
dist/
.next/
coverage/
.DS_Store
*.log

# env
.env
.env.*
!.env.example

# prisma
prisma/*.db
prisma/*.db-journal

# storage
storage/

# prod DB dumps (auto-backup из scripts/deploy-prod.sh) — данные прод не версионируем
backups/*.sql.gz
backups/*.dump
# бэкап-дампы БД тенантов перед удалением (scripts/tenants/delete-tenant.ts)
backups/tenants/

# patterns uploads (модуль «Лекала», MVP-1)
apps/api/uploads/

# uploads-артефакты прогона integration-тестов
tests/apps/api/uploads/

# editor
.idea/
.vscode/

# TypeScript build info
*.tsbuildinfo
apps/api/apps/

# Claude Code harness runtime lock (not source)
.claude/scheduled_tasks.lock

# входящие файлы на разбор (xlsx и т.п.) — в git не кладём, только README
docs/upload/*
!docs/upload/README.md

# PDF-экспорты макетов — производные от HTML рядом, в git не кладём
docs/mockups/*.pdf
