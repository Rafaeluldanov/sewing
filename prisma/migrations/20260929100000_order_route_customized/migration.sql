-- Маршрут заказа правили холстом вручную («Маршрут операций» → «Изменить маршрут»).
-- Выключатель ре-синка снимка `OrderRouteStep[]` из шаблона: пока NULL, шаблон
-- остаётся источником истины и правка шаблона доезжает до незапущенных заказов;
-- после ручной правки ре-синк отключается, иначе «Пересчитать план операций»
-- вернул бы маршрут к шаблону. Сбрасывается при осознанной смене шаблона в заказе.
-- NULL для всех существующих заказов — их маршрут ещё не правили.
ALTER TABLE "Order" ADD COLUMN "routeCustomizedAt" TIMESTAMP(3);
