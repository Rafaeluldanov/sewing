-- Служебный сотрудник для машинного токена ERP (правило §0.1 в service/docs/kb/sewing.md).
--
-- У полей «кто создал / кто принял / кто завершил расчёт» внешний ключ на Employee, а машинный
-- токен сотрудником не является — запись из ERP падала бы на ключе. Колонка nullable и
-- приезжает пустой: сотрудник создаётся лениво при первом запросе токена, существующие
-- токены продолжают работать без правки данных.
ALTER TABLE "ServiceToken" ADD COLUMN IF NOT EXISTS "employeeId" TEXT;
