# Node 20+ — см. подробный комментарий в Dockerfile.prod (engines + global `File`).
FROM node:20-alpine
WORKDIR /app
COPY package*.json ./
COPY apps/web/package*.json ./apps/web/
RUN npm ci
WORKDIR /app/apps/web
EXPOSE 3000
CMD ["npx", "next", "dev", "-p", "3000"]
